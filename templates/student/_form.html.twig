{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.firstName) }}
    {{ form_row(form.lastName) }}
    {{ form_row(form.email) }}
    {{ form_row(form.dateOfBirth) }}
    {{ form_row(form.classGroup) }}
    {{ form_row(form.projects) }}
{{ form_end(form) }}